<%= User.find(params[:id]).firstname %> <%= User.find(params[:id]).lastname %>
<div class="user">
  <br>

  <h1 style="position: relative; left: 2.6em"></h1>
  <%= image_tag current_user.url %>
  <ul>
    <%= render 'form' %>
  </ul>
</div>

