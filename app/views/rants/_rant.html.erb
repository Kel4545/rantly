
<p>
<li><%= link_to truncate(rant.rant, :length => 300, :seperator => '..'), user_rant_path(rant.user_id, rant.id) %></li>
<%= link_to "Delete", user_rant_path(rant.user_id, rant.id), :class => "delete", :method => :delete %></p>

