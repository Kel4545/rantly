<%=User.find(params[:user_id]).firstname %>
<%= link_to "Following", user_rants_path(@user.id), :class => "button following" %>
<% User.find(params[:user_id]).rants.each do |rant| %>
  <%= rant.rant %>
<% end %>